<Window x:Class="LabelCreator.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LabelCreator"
        xmlns:helpers="clr-namespace:LabelCreator.Helpers"
        mc:Ignorable="d"
        Title="MainWindow"
        Width="700" 
        MinWidth="300"
        Height="400"
        MinHeight="200"
        >
    
    <Window.CommandBindings>
        <CommandBinding Command="helpers:CommandHandler.NewCanvas" Executed="Command_NewCanvas"></CommandBinding>
    </Window.CommandBindings>    
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>  <!--Menu Plik-->
            <RowDefinition Height="22"/>    <!--Pasek Toolbox-->
            <RowDefinition Height="*"/>     <!--Kontent-->
            <RowDefinition Height="48"/>    <!--Canvas zoom-->
            <RowDefinition Height="auto"/>  <!--Status bar-->
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Menu Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2">
            <MenuItem Header="Plik">
                <MenuItem Header="Zapisz"></MenuItem>
                <MenuItem Header="Otwórz"></MenuItem>
            </MenuItem>
            <MenuItem Header="Program">
                <MenuItem Header="Nowa etykieta" Command="" ></MenuItem>                
            </MenuItem>
        </Menu>

        <ToolBar Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Padding="0" Background="#FFF3F3F3">
            <Button Height="20" Click="Button_Click">Tekst</Button>
            <Button Height="20" Click="Button_Click">Obraz</Button>
            <Button Height="20" Click="Button_Click">Kod kreskowy</Button>
        </ToolBar>

        <ScrollViewer Grid.Row="2" Grid.Column="0" Grid.RowSpan="2" Margin="0" VerticalScrollBarVisibility="Auto">
            <GroupBox  Header="Komponenty">
                <ListBox BorderBrush="{x:Null}">
                    
                </ListBox>
            </GroupBox>
        </ScrollViewer>

        <ScrollViewer Grid.Row="2" Grid.Column="1" Margin="0" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
            <GroupBox Header="Obszar roboczy">
                <Canvas Background="#FFEBEBEB" Name="DesigningCanvas" Height="250" Width="300" HorizontalAlignment="Left" VerticalAlignment="Top" MouseWheel="DesigningCanvas_MouseWheel">
                    <Canvas.LayoutTransform>
                        <ScaleTransform x:Name="CanvasScaleTransform"/>
                    </Canvas.LayoutTransform>
                </Canvas>
            </GroupBox>
        </ScrollViewer>

        <GroupBox Grid.Row="3" Grid.Column="1" Header="Zoom">
            <Slider Name="SliderCanvasZoom" Value="1" Minimum="0.2" Maximum="3" TickFrequency="0.1" TickPlacement="BottomRight" ValueChanged="SliderCanvasZoom_ValueChanged" AutoToolTipPlacement="BottomRight" AutoToolTipPrecision="2"></Slider>
        </GroupBox>
        

        <StatusBar Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="2">Status bar</StatusBar>
    </Grid>
</Window>
